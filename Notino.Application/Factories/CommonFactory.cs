using Notino.Application.Contracts.Persistence;
using System;
using System.Collections.Generic;
using System.Text;

namespace Notino.Application.Factories
{
    public class CommonFactory
    {
        //public T Get<T>(string className) //where T : IDocumentRepository
        //{
        //    //it create object using Template type T and by making use of reflection
        //    //once create factory this way there is no need to open it again for modification
        //    Type type = typeof(T);
        //    Object obj = Activator.CreateInstance(type,);
        //    return (T)obj;
        //}
    }
}
